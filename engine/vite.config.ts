import { defineConfig } from "vite";

// base: "./" — билд разворачивается статикой в произвольном подпути сайта
// (Render раздаёт весь репозиторий как есть, без build-шага, см. render.yaml
// в корне), поэтому пути к ассетам не должны быть жёстко привязаны к корню
// домена.
export default defineConfig({
  base: "./",
});
